<h1>Listing ufs</h1>

<table>
  <tr>
    <th>Sigla</th>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ufs.each do |uf| %>
  <tr>
    <td><%= uf.sigla %></td>
    <td><%= uf.nome %></td>
    <td><%= link_to 'Show', uf %></td>
    <td><%= link_to 'Edit', edit_uf_path(uf) %></td>
    <td><%= link_to 'Excluir', uf, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Uf', new_uf_path %>
